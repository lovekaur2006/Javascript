// Function Expression
let calSub = function (x, y) {
    let z = x - y;
    return z;
}
console.log("Subtration : " + calSub(9, 6));

//---------------------------------

let calMul = function (x, y) {
    let z = x * y;
    return z;
}
console.log("Multiplication : " + calMul(5, 4));
