// Function parameters:- These are the names listed in the function defination.

// Function Arguements:- The values that are declared within a function when a function is called.

/*function functionName(parameter1, parameter2,
    parameter3{

        code to be executed

    })
    
*/
function sum(a, b, c) {
    return a + b + c;
}
let x = sum(1, 2, 3);
console.log(x);

//----> Here, in the above example a, b , c ae the parameters and 1, 2, 3 are the arguements.

